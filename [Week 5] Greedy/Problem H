#include <iostream>
 
using namespace std;
int a[5] = {100, 20, 10, 5, 1};
int main(){
	int n, cnt = 0, i = 0;
	cin >> n;
	while(n){
		while(n >= a[i]){
			cnt++;
			n -= a[i];
		}
		i++;
	}
	cout << cnt;
}
